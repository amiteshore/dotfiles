#!/bin/bash

cyan=$(tput setaf 14);
yellow=$(tput setaf 226);
reset=$(tput sgr0);

PS1="\[${cyan}\]\w "; # working directory & whitespace
PS1+="\[${yellow}\]✗ "; # ✗ & whitespace
PS1+="\[${reset}\]"; # reset color

export PS1;